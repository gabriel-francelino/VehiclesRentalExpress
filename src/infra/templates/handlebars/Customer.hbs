<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Customer: {{name}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #99670a;
            margin: 0;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }
        div {
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
        }
        h1 {
            background-color: #99670a;
            color: #fff;
            padding: 10px 15px;
            margin: 0;
            border-radius: 10px;
        }
        p {
            padding: 15px;
            margin: 0;
            color: #312d2d;
        }
        strong {
            color: #0f0f0f;
        }
    </style>
</head>
<body>
    <div>
        <h1>Customer: {{name}}</h1>
        <p><strong>Id:</strong> {{id}}</p>
        <p><strong>Name:</strong> {{name}}</p>
        <p><strong>Email:</strong> {{email}}</p>
        <p><strong>CPF:</strong> {{cpf}}</p>
        <p><strong>Date of birth:</strong> {{dateOfBirth}}</p>
        <p><strong>Driver license:</strong> {{DriverLicense}}</p>
    </div>
</body>
</html>